# the standard variables don't make sense for an uninstalled copy
prefix=
exec_prefix=
libdir=${pcfiledir}/..
includedir=${pcfiledir}/..

Name: MOGAL Uninstalled
Description: Multi-Objective Genetic Algorithm Library, Not Installed
Version: @VERSION@
Requires:
Libs: ${libdir}/src/libmogal.la @MOGALLINKLIBS@
Cflags: -I${includedir}/src @MOGALINCLUDES@
